# Tests

Tests will be added with each implementation layer:

- DWB-equivalent behavior with certification disabled
- acceleration candidate generation and rollout
- jerk state transitions and reset behavior
- FIR buffer transitions and terminal settling
- nominal-delay preview, terminal stop, and retained backup behavior
- plugin loading through the Nav2 Controller Server
